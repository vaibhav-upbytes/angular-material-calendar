import { createAction } from '@ngrx/store';

export const increment = createAction('[Date Component] Increment');
export const decrement = createAction('[Date Component] Decrement');
export const reset = createAction('[Date Component] Reset');
import { createAction } from '@ngrx/store';

export const month = createAction('[view Component] month');
export const week = createAction('[view Component] week');
export const day = createAction('[view Component] day');
